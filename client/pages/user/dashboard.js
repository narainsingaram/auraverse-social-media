import { useContext } from 'react';
import { UserContext } from '../../context';

const Dashboard = () => {
    return (
        <div>
            fortnite balls
        </div>
    );
};

export default Dashboard;
